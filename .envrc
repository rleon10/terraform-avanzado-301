# Carga .env si existe (local o Codespaces con .env creado a mano).
# No depende de dotenv_if_exists (falla en algunos Codespaces).
watch_file .env
if [ -f .env ]; then
  set -a
  # shellcheck disable=SC1091
  source .env
  set +a
fi
